import React from 'react'
import { View, Text, Button } from 'react-native'


export const LandingScreen = () => {

    return (
        <View>
            <Button title='START' onPress={() => ''} />
            <Button title='RESULT' onPress={() => {}} />
        </View>
    )
}